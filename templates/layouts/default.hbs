<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{title}}</title>
  <link rel="stylesheet" href="css/default.css">
</head>
<body>
<h1>{{title}}</h1>


{{#components}}
{{#each items as |item|}}
 <a href="{{autolink}}">{{item.data.title}}</a>
{{/each}}
{{/components}}

{% body %}
</body>
</html>